# PostgreSQL connection string — required
# Use a cloud database: Neon (free), Supabase, or Railway
# Format: postgresql://user:password@host:5432/dbname
DATABASE_URL=postgresql://postgres:password@localhost:5432/hrms

# Comma-separated list of allowed frontend origins (no trailing slash)
# Use * to allow all (fine for development / public API)
# Example: https://hrms-portal.vercel.app,https://www.example.com
CORS_ORIGINS=*
